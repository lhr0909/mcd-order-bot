rules:
  # - rule: out-of-scope
  #   steps:
  #   - intent: fallback
  #   - action: utter_fallback
  - rule: handle order_failed
    condition:
    - slot_was_set:
      - state: 'order_failed'
    steps:
    - action: utter_fallback
